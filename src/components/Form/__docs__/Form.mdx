import { Canvas, Meta } from '@storybook/blocks';
import Example from './Example.tsx';
import * as Form from './Form.stories.tsx';

<Meta of={Form} title='Form' />

# Form

Form component.

#### Example

<Canvas of={Form.Primary} />

## Usage

```ts
import {Form} from "react-blog-builder";

export default function Example() {
  return (
      style={{ color: 'black' }}
  );
};

export default Example;
```

#### Arguments

- **styles** - CSS Styles to a apply to the component.
